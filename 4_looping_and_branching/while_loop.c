#include <stdio.h>

// learning while loop
// // countdown to START RUNNING
int main(void)
{
	int cnt = 10;
	while(cnt >=1); {
		printf("%d \n", cnt);
		cnt--;
	}
	printf("\n>>START RUNNING<<\n");

return(0);
}
